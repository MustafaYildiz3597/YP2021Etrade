<!-- Modal Form -->
<div id="modalBrowseBUKODPopup" class="modal-block modal-block-primary mfp-hide" style="max-width: 800px;">
    <section class="card">
        <header class="card-header bg-primary text-white">
            <div class="row">
                <div class="col-10 d-flex align-items-center">
                    <h2 class="card-title text-white" ng-bind="'BU Numarası Seç'"></h2>
                </div>
                <div class="col-2 text-right d-flex align-items-center justify-content-end">
                    <button class="btn btn-primary pull-right kapat-btn" ng-click="CloseBrowseBUKOD()"><i class="fas fa-times text-white" style="margin:0px"></i></button>
                </div>
            </div>
        </header>

        <div class="card-body">
            <div class="panel panel-default">
                <div class="panel-body">
                    @*<div class="row">
            <div class="col-xs-2">
                <button class="btn btn-primary pull-right" ng-click="ShowMUGridEditPopup()"><i class="fas fa-upload text-white"> Düzenle (GridEdit)</i></button>
                <button class="btn btn-primary pull-right" ng-click="ShowMUGridAddPopup()"><i class="fas fa-upload text-white"> Ekle (GridAdd)</i></button>
            </div>
        </div>*@
                    <div class="row">
                        <div class="col-6 mb-20 d-flex align-items-center">
                            <h3 style="margin-top:5px">Sayfa: {{ currentPage2 }}</h3>
                        </div>
                        <div class="col-6 mb-20 d-flex align-items-center justify-content-end">
                            <div class="form-group row">
                                <div class="col-12">
                                    <input ng-model="q2" id="search2" class="form-control" placeholder="Ara">
                                </div>
                            </div>
                        </div>

                        <div class="col-6 mb-20 d-flex align-items-center">
                            <label style="margin-right:10px; margin-bottom:0px">Kayıt Adedi: </label>
                            <span ng-show="q2 != undefined && q2 != ''" ng-bind="pfiltered.length.toString() + ' (filtre) /'"></span> <span ng-bind="BrowseProducts.length"></span>
                        </div>
                        <div class="col-6 mb-20 d-flex align-items-center justify-content-end">
                            <label for="search" style="margin-bottom:0px">items per page:</label>
                            <select ng-model="pageSize2" ng-options="o for o in pagesSizes"></select>
                        </div>
                    </div>

                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Seç</th>
                                @*<th ng-click="sort2('MPID')">MPID <span class="glyphicon sort-icon pull-right" ng-show="sortBy2=='PRODID'" ng-class="{'glyphicon-chevron-up':reverse2,'glyphicon-chevron-down':!reverse2}"></span></th>*@
                                <th ng-click="sort2('BUKOD')">BUKOD <span class="glyphicon sort-icon pull-right" ng-show="sortBy2=='BUKOD'" ng-class="{'glyphicon-chevron-up':reverse2,'glyphicon-chevron-down':!reverse2}"></span></th>
                                <th ng-click="sort2('NAME')">Ürün <span class="glyphicon sort-icon pull-right" ng-show="sortBy2=='NAME'" ng-class="{'glyphicon-chevron-up':reverse2,'glyphicon-chevron-down':!reverse2}"></span></th>
                                <th>XPSNO</th>
                                <th>PRICE</th>
                                <th>Code</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr dir-paginate="pitem in pfiltered = (BrowseProducts|orderBy:sortBy2:reverse2 | filter:q2 ) | itemsPerPage: pageSize2" pagination-id="p_list" current-page="currentPage2">
                                <td @*ng-show="SelMode!='m'"*@><input type="radio" name="sel" ng-model="$parent.SelProduct" value="{{pitem.PRODID}}"></td>
                                @*<td ng-show="SelMode=='m'"><input type="checkbox" ng-model="pitem.chk"></td>*@
                                @*<td ng-bind="pitem.PRODID"></td>*@
                                <td ng-bind="pitem.BUKOD"></td>
                                <td ng-bind="pitem.NAME_EN"></td>
                                <td ng-bind="pitem.XPSNO"></td>
                                <td ng-bind="pitem.PRICE"></td>
                                <td ng-bind="pitem.Code"></td>

                            </tr>
                        </tbody>
                    </table>

                    <div @*ng-controller="OtherController" class="other-controller"*@>
                        @*<small>this is in "OtherController"</small>*@
                        <div class="text-center">
                            <dir-pagination-controls pagination-id="p_list" boundary-links="true" @*on-page-change="pageChangeHandler(newPageNumber)"*@></dir-pagination-controls>
                            <button class="btn btn-primary pull-right" @*ng-show="SelMode=='s'"*@ ng-click="AddSelectedBUKOD()"><i class="fas fa-check text-white"></i></button>
                            @*<button class="btn btn-primary pull-right" ng-show="SelMode=='m'" ng-click="AddMultiBUKODToAddGrid()"><i class="fas fa-arrow-right"></i> Seçilenleri Listeye Ekle</button>
                <button class="btn btn-primary pull-right" ng-show="SelMode=='e'" ng-click="AddBUKODToEditPage()"><i class="fas fa-check text-white"></i></button>*@
                        </div>
                    </div>
                    @*{{GridAddModel}}*@

                </div>
            </div>



        </div>
    </section>
</div>



